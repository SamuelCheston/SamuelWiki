# HRPAuth Wiki Roadmap

## 项目目标

把当前项目建设成一个面向多项目文档沉淀的 Wiki 站点。站点需要能够稳定读取 `wiki/` 目录下的 Markdown 内容，并以清晰的导航、搜索、阅读和维护体验对外展示。

## 内容结构约束

基于当前约束，建议把 `wiki/` 作为纯内容根目录，并固定以下规则：

1. `wiki/` 第一层目录不直接存放 `.md` 文件。
2. 所有 Markdown 文件只放在第二层项目目录内。
3. 每个第二层目录代表一个独立项目的 Wiki。

建议目录形态：

```text
wiki/
  business/
    hrp-auth/
      index.md
      quick-start.md
      faq.md
  platform/
    auth-gateway/
      index.md
      architecture.md
      release-note.md
```

建议约定：

1. 第一层目录用于分组，例如业务线、团队、领域。
2. 第二层目录名作为项目唯一 slug，用于路由生成。
3. 每个项目目录至少包含一个 `index.md`，作为项目 Wiki 首页。
4. 图片、附件等静态资源优先放在项目目录内，避免跨项目引用。

## 产品路线

### Phase 0：内容约束落地

目标：先把内容组织规则定清楚，让后续开发围绕稳定的数据来源展开。

交付物：

1. 明确 `wiki/<group>/<project>/*.md` 的目录规范。
2. 定义 Markdown Frontmatter 字段，例如 `title`、`description`、`order`、`tags`、`updatedAt`。
3. 提供 1 到 2 个示例项目目录，验证约束是否够用。

验收标准：

1. 新增内容时不需要猜目录应该怎么建。
2. 程序可以无歧义地扫描并识别全部项目和页面。

### Phase 1：最小可用 Wiki 站点

目标：先把“能读、能看、能导航”做出来。

交付物：

1. 首页：展示所有项目 Wiki 卡片。
2. 项目页：展示项目概览、文档目录、最近更新。
3. 文档页：渲染 Markdown 内容。
4. 左侧或顶部导航：支持项目切换与页内目录浏览。
5. 404 页面：处理不存在的项目或文档路径。

验收标准：

1. 从首页可以进入任意项目 Wiki。
2. 项目内页面能通过导航互相跳转。
3. Markdown 标题、列表、代码块、引用块渲染正常。

### Phase 2：阅读体验增强

目标：把站点从“能用”提升到“好用”。

交付物：

1. 自动提取标题生成文档目录。
2. 代码块高亮与复制按钮。
3. 面包屑、上一篇/下一篇导航。
4. 暗色模式适配。
5. 页面更新时间、标签、阅读时长等元信息展示。

验收标准：

1. 长文档可快速定位章节。
2. 技术文档阅读体验接近成熟文档站。

### Phase 3：内容检索与治理

目标：解决“找不到”和“越写越乱”的问题。

交付物：

1. 全站搜索，支持标题、标签、正文关键字。
2. Frontmatter 校验与构建期报错。
3. 目录规则校验，阻止非法 Markdown 路径进入站点。
4. 死链检查与缺失资源提示。
5. 统一的排序、标签和归档策略。

验收标准：

1. 用户可通过搜索快速找到目标文档。
2. 内容结构不符合规范时，开发阶段可直接发现。

### Phase 4：工程化与可持续维护

目标：让 Wiki 站点成为可长期运营的项目，而不是一次性页面集合。

交付物：

1. 内容扫描、路由生成和元数据生成的独立模块。
2. 基础测试覆盖：目录扫描、Frontmatter 解析、路由映射。
3. CI 流程：构建、Lint、内容校验。
4. 部署方案与环境配置。
5. 错误页、空态页、加载态等完整体验。

验收标准：

1. 新增项目 Wiki 不需要手写路由配置。
2. 内容改坏时能在 CI 或本地开发阶段被拦住。

## 技术路线建议

### 1. 路由设计

建议使用如下 URL 结构：

```text
/
/projects
/projects/:group/:project
/projects/:group/:project/:pageSlug
```

说明：

1. `group` 对应 `wiki/` 第一层目录。
2. `project` 对应第二层项目目录。
3. `pageSlug` 由 Markdown 文件名生成，`index.md` 可映射为项目首页。

### 2. 数据来源

建议优先采用本地文件扫描方案，在构建期生成站点所需元数据：

1. 扫描 `wiki/` 目录。
2. 解析 Frontmatter 和 Markdown 正文。
3. 生成项目列表、页面树、路由表和搜索索引。

这样前期实现简单，且和“仓库即内容源”的目标一致。

### 3. Markdown 能力

优先级建议：

1. 基础 Markdown 渲染。
2. Frontmatter 解析。
3. 标题锚点与目录生成。
4. 代码块高亮。
5. GitHub Flavored Markdown 扩展。

### 4. UI 结构

基于 Chakra UI，建议优先做这几个稳定页面骨架：

1. `HomePage`
2. `ProjectListPage`
3. `ProjectWikiPage`
4. `DocPage`
5. `NotFoundPage`

同时抽出几个基础组件：

1. `AppShell`
2. `SidebarNav`
3. `DocToc`
4. `MarkdownRenderer`
5. `ProjectCard`
6. `SearchBox`

## 推荐实施顺序

1. 先补内容规范与示例目录。
2. 再做 Markdown 扫描与路由映射。
3. 接着完成首页、项目页、文档页三类核心页面。
4. 之后补目录、搜索、元信息和体验增强。
5. 最后补内容校验、测试和部署。

## 近期两周可执行版本

### Week 1

1. 定义目录规范和 Frontmatter 规范。
2. 接入 Markdown 解析链路。
3. 完成 Wiki 首页和项目详情页静态骨架。
4. 准备 1 个示例项目 Wiki。

### Week 2

1. 打通 Markdown 文档页渲染。
2. 补项目导航与文档目录。
3. 增加目录校验和基础空态。
4. 完成一次端到端手工验收。

## 当前建议的下一步

最值得马上开始的是下面三件事：

1. 先定 `wiki/<group>/<project>/` 目录协议和 Frontmatter 字段。
2. 选定 Markdown 渲染与文件扫描方案。
3. 按 Phase 1 直接实现首页、项目页、文档页三个核心页面。
